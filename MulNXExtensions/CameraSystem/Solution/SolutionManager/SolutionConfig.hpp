#pragma once

class SolutionConfig {
public:
    //是否启用解决方案快捷键
    bool SolutionShortcutEnable = false;
    //是否绘制播放摄像机
    bool PlayingDraw = false;
    //是否启用播放摄像机覆盖
    bool PlayingOverride = false;
};